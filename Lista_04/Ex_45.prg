/*

45. O programa abaixo possui erros de sintaxe e lógica. Erros de sintaxe
são aqueles relativos às regras do VisualG. Já os erros de lógica
indicam um funcionamento inesperado do algoritmo. Analise o
programa abaixo e indique através de comentários esses erros.


RESPOSTA

Em "Escreva(Os números são iguais)", deveria ter aspas entre a mensagem,
ficando: Escreva("Os números são iguais").

Se A > B, que lê-se "Se A for maior que B" e a mensagem que ele escreveu foi 
(A, " É menor que ", B), ou seja, a posição das variáveis (A e B) estão invertidas.

O mesmo ocorre no print do Senão, a posição das variáveis (A e B) estão invertidas. 

*/